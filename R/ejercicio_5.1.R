# En la segunda columna de la tabla se encuentra el campo Response.  Resistant, pre treatment


#En el ejercicio 3, si no se tuviera el 0 en la formula, se consideraraía el intercepto en el modelo
#Esto no es recomendado cuando en el modelo se usan batches, debido a que se perdería el primer batch.
#Con la perdida de este batch, se modifica toda la interpretación.
